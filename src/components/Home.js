import React, {useState} from "react";

const Home = (props) => {
  return (
    <main className="main-home">
      <div className="main-container">
        // TODO: Add components
      </div>
    </main>
  )
};

export default Home;